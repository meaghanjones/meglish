<h1>New Cateogory for courses</h1>
<%= render "layouts/errors", :object => @category %>
<%= form_for @category do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, class: "form-control" %>

  <%= f.submit class: "btn btn-primary" %>
<% end %>
